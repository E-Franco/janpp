import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'janppsaude.janpp.com.br',
  appName: 'JANPP SAUDE',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
